/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.prod.website-files.com',
            'media.istockphoto.com',

        ]
    }
};

export default nextConfig;
