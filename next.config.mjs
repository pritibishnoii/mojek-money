/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.prod.website-files.com',
            'example.com',
            'another-domain.com',
        ]
    }
};

export default nextConfig;
